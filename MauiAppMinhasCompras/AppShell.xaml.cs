namespace MauiAppMinhasCompras // Define um namespace para a aplicação principal.
{
    public partial class AppShell : Shell // Define a classe AppShell que herda de Shell, que é usada para navegação.
    {
        public AppShell() // Construtor da classe AppShell.
        {
            InitializeComponent(); // Inicializa os componentes definidos no arquivo de interface.
        }
    }
}
